const URL = "mongodb://localhost:27017";
const DATA_NAME = 'apiData'
const TABLE_NAME = "site"

const config = {
    url: URL,
    database: DATA_NAME,
    table: TABLE_NAME
}
module.exports = config;
